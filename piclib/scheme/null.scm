(define-library (scheme null)
  (import (scheme base))
  (export define
          lambda
          if
          quote
          quasiquote
          unquote
          unquote-splicing
          begin
          set!
          define-syntax))
